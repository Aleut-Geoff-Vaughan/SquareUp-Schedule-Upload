[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
# Put the repo root on sys.path so tests can import the app modules
# (app, database, square_api, security, timezones, ...) at collection time,
# regardless of whether pytest is invoked as `pytest` or `python -m pytest`.
pythonpath = .
