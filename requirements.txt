# Pinned to current, CVE-patched releases. These are the exact versions the
# test suite is validated against.
Flask==3.1.3
Flask-CORS==6.0.5
Werkzeug==3.1.8
requests==2.33.1
gunicorn==23.0.0
# Optional: enables native .xlsx uploads in AI Convert. Safe to remove if you
# only ever upload CSV/TSV/TXT.
openpyxl==3.1.5
pytest==8.3.5
