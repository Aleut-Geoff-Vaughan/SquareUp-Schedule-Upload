# Runtime dependencies only. Pinned to current, CVE-patched releases.
# Test/dev tooling lives in requirements-dev.txt so it isn't shipped in the
# production image or flagged by the dependency audit.
Flask==3.1.3
Flask-CORS==6.0.5
Werkzeug==3.1.8
requests==2.33.1
gunicorn==23.0.0
# Optional: enables native .xlsx uploads in AI Convert. Safe to remove if you
# only ever upload CSV/TSV/TXT.
openpyxl==3.1.5
